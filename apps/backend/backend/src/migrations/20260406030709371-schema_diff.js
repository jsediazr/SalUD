/* Auto-generated by sequelize-schema-diff.js */
'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    await queryInterface.removeConstraint('detalles_cita', 'detalles_cita_id_cita_fkey').catch(() => {});

    await queryInterface.addConstraint('detalles_cita', {
      fields: ['id_cita'],
      type: 'foreign key',
      name: 'detalles_cita_id_cita_fkey',
      references: { table: 'citas', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('citas', 'citas_id_paciente_fkey').catch(() => {});

    await queryInterface.addConstraint('citas', {
      fields: ['id_paciente'],
      type: 'foreign key',
      name: 'citas_id_paciente_fkey',
      references: { table: 'pacientes', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('citas', 'citas_id_doctor_fkey').catch(() => {});

    await queryInterface.addConstraint('citas', {
      fields: ['id_doctor'],
      type: 'foreign key',
      name: 'citas_id_doctor_fkey',
      references: { table: 'doctores', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('citas', 'citas_id_horario_fkey').catch(() => {});

    await queryInterface.addConstraint('citas', {
      fields: ['id_horario'],
      type: 'foreign key',
      name: 'citas_id_horario_fkey',
      references: { table: 'horarios', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.addColumn('doctores', 'especialidad', {
      type: Sequelize.STRING
    });

    await queryInterface.sequelize.query('CREATE UNIQUE INDEX IF NOT EXISTS "doctores_idUsuario_unique" ON "doctores" ("id_usuario");');

    await queryInterface.removeConstraint('doctores', 'doctores_id_usuario_fkey').catch(() => {});

    await queryInterface.addConstraint('doctores', {
      fields: ['id_usuario'],
      type: 'foreign key',
      name: 'doctores_id_usuario_fkey',
      references: { table: 'usuarios', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('ordenes', 'ordenes_id_cita_fkey').catch(() => {});

    await queryInterface.addConstraint('ordenes', {
      fields: ['id_cita'],
      type: 'foreign key',
      name: 'ordenes_id_cita_fkey',
      references: { table: 'citas', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('pacientes', 'pacientes_id_usuario_fkey').catch(() => {});

    await queryInterface.addConstraint('pacientes', {
      fields: ['id_usuario'],
      type: 'foreign key',
      name: 'pacientes_id_usuario_fkey',
      references: { table: 'usuarios', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('roles_usuario', 'roles_usuario_id_rol_fkey').catch(() => {});

    await queryInterface.addConstraint('roles_usuario', {
      fields: ['id_rol'],
      type: 'foreign key',
      name: 'roles_usuario_id_rol_fkey',
      references: { table: 'rol', field: 'id' },
      onDelete: 'CASCADE',
      onUpdate: 'CASCADE'
    });

    await queryInterface.removeConstraint('roles_usuario', 'roles_usuario_id_usuario_fkey').catch(() => {});

    await queryInterface.addConstraint('roles_usuario', {
      fields: ['id_usuario'],
      type: 'foreign key',
      name: 'roles_usuario_id_usuario_fkey',
      references: { table: 'usuarios', field: 'id' },
      onDelete: 'CASCADE',
      onUpdate: 'CASCADE'
    });

    await queryInterface.changeColumn('horarios', 'hora_inicio', {
      type: Sequelize.TIME
    });

    await queryInterface.changeColumn('horarios', 'hora_fin', {
      type: Sequelize.TIME
    });

    await queryInterface.removeConstraint('horarios', 'horarios_id_doctor_fkey').catch(() => {});

    await queryInterface.addConstraint('horarios', {
      fields: ['id_doctor'],
      type: 'foreign key',
      name: 'horarios_id_doctor_fkey',
      references: { table: 'doctores', field: 'id' },
      onDelete: 'SET NULL',
      onUpdate: 'CASCADE'
    });
  },

  down: async (queryInterface, Sequelize) => {
    await queryInterface.removeIndex('doctores', 'doctores_idUsuario_unique').catch(() => {});
    await queryInterface.removeColumn('doctores', 'especialidad').catch(() => {});
  }
};
